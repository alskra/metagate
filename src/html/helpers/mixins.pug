include ../../../node_modules/bemto.pug/bemto.pug
-
  set_bemto_settings({
    prefix: '',
    element: '__',
    modifier: '--'
  })

mixin isvg(id)
  - attributes.id ? id = attributes.id : null; attributes.id = undefined;
  +b.SVG(class=`isvg-${id}` role="img")&attributes(attributes)
    use(xlink:href=`#isvg-${id}`)